# Biến môi trường cho bot Telegram HUTECH
# Sao chép file này thành .env cho phát triển local
# (không được commit lên git)

# Token của bot Telegram (lấy từ @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Cấu hình database PostgreSQL
# Thay thế bằng URL kết nối thực tế của bạn
POSTGRES_URL="postgresql://user:password@postgres:5432/db_name"

# Cấu hình Redis
REDIS_URL="redis://redis:6379/cache_name"

# Cấu hình logging
# LOG_LEVEL: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# LOG_JSON: true/false (nên bật true ở production để dễ parse)
LOG_JSON=false

# Cấu hình polling Telegram (để giảm lỗi NetworkError/ReadError khi mạng chập chờn)
# Timeout chung cho các API call khác getUpdates
TELEGRAM_CONNECT_TIMEOUT=10
TELEGRAM_READ_TIMEOUT=20
TELEGRAM_WRITE_TIMEOUT=20
TELEGRAM_POOL_TIMEOUT=5
TELEGRAM_CONNECTION_POOL_SIZE=16

# Timeout riêng cho long-polling getUpdates
TELEGRAM_GET_UPDATES_CONNECT_TIMEOUT=10
TELEGRAM_GET_UPDATES_READ_TIMEOUT=35
TELEGRAM_GET_UPDATES_WRITE_TIMEOUT=20
TELEGRAM_GET_UPDATES_POOL_TIMEOUT=5

# Tham số polling
TELEGRAM_POLL_TIMEOUT=10
TELEGRAM_POLL_INTERVAL=0.5
# -1: retry vô hạn khi bootstrap/startup gặp lỗi mạng
TELEGRAM_BOOTSTRAP_RETRIES=-1
